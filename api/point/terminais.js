// ── /api/point/terminais ─────────────────────────────────────────────────────
// Lógica em _lib/point-handlers.js (testada com dependências falsas).
module.exports = require("../_lib/point-runtime").terminais;
